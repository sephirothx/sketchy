# Sketchy

The repository instructions live in [AGENTS.md](AGENTS.md) — the reference docs to
read before implementing, the staleness checklist to run before committing, the
invariants, and the branch naming rules. They are kept in one file so that every tool
reads the same rules; this file imports them rather than repeating them.

@AGENTS.md
