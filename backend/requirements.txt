aiosqlite==0.22.1
alembic==1.19.1
anyio==4.14.2
argon2-cffi-bindings==25.1.0
argon2-cffi==25.1.0
asyncpg==0.31.0
# WebAuthn's own transitive set (R-AUTH-23). Six packages where the rest of
# this file is argon2 plus the SQLAlchemy stack, which N-15 counted as the
# cost of phishing resistance and is now the price being paid: `webauthn`
# needs `cryptography` to check a signature, `cbor2` to read what an
# authenticator sends, and `pyOpenSSL`/`pyasn1` for the attestation
# certificates this deployment asks for none of.
cbor2==6.1.4
cffi==2.1.1
click==8.4.2
cryptography==50.0.1
fastapi==0.139.2
greenlet==3.5.4
h11==0.16.0
Mako==1.4.1
MarkupSafe==3.0.3
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pyOpenSSL==26.4.0
python-engineio==4.13.3
python-socketio==5.16.3
SQLAlchemy[asyncio]==2.0.52
starlette==1.3.1
uvicorn==0.51.0
webauthn==3.0.0
# The WebSocket implementation app.server names; see app/ws_transport.py.
wsproto==1.3.2
